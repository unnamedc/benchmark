# HTTP Server Availability Tester

Консольная программа для тестирования доступности и измерения времени ответа HTTP серверов.

## Требования

- Python 3.7+
- httpx 0.24.1 (или requests/aiohttp)

## Установка

```bash
# Клонирование репозитория
git clone <repository_url>
cd http-bench

# Создание виртуального окружения (рекомендуется)
python -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

## Использование

### Основной синтаксис

```bash
python bench.py -H <hosts> -C <count> [-O <output_file>]
python bench.py -F <file> -C <count> [-O <output_file>]
```

### Параметры

- `-H, --hosts` - Список хостов через запятую без пробелов (например: `https://ya.ru,https://google.com`)
- `-F, --file` - Путь к файлу со списком хостов (по одному в строке)
- `-C, --count` - Количество запросов на каждый хост (по умолчанию: 1)
- `-O, --output` - Путь для сохранения результатов (опционально, без указания выводит в консоль)

**Примечание:** Можно использовать либо `-H`, либо `-F`, но не оба одновременно.

### Примеры запуска

**Пример 1: Тестирование двух хостов с 5 запросами на каждый**

```bash
python bench.py -H https://ya.ru,https://google.com -C 5
```

**Пример 2: Загрузка хостов из файла**

```bash
python bench.py -F hosts.txt -C 10
```

**Пример 3: Сохранение результатов в файл**

```bash
python bench.py -H https://example.com -C 5 -O results.txt
```

## Формат входного файла

Файл `hosts.txt` должен содержать по одному адресу в строке:

```
https://ya.ru
https://google.com
https://example.com
```

## 📊 Пример вывода

### Справка программы

```bash
$ python bench.py -h

usage: bench.py [-h] (-H HOSTS | -F FILE) [-C COUNT] [-O OUTPUT]

HTTP Server Availability Tester

options:
  -h, --help            show this help message and exit
  -H HOSTS, --hosts HOSTS
                        Comma-separated list of hosts (e.g., https://ya.ru,https://google.com)
  -F FILE, --file FILE  Path to file with list of hosts (one per line)
  -C COUNT, --count COUNT
                        Number of requests per host (default: 1)
  -O OUTPUT, --output OUTPUT
                        Path to output file (default: console)

Examples:
  python bench.py -H https://ya.ru,https://google.com -C 5
  python bench.py -F hosts.txt -C 10 -O results.txt
```

### Результаты бенчмарка

```bash
$ python bench.py -H https://ya.ru,https://google.com -C 5

Starting benchmark: 2 host(s), 5 request(s) each...
======================================================================
HTTP Server Benchmark Results
======================================================================

Host: https://ya.ru
----------------------------------------------------------------------
  Success:  5
  Failed:   0
  Errors:   0
  Min:      609.61 ms
  Max:      624.70 ms
  Avg:      613.03 ms

Host: https://google.com
----------------------------------------------------------------------
  Success:  5
  Failed:   0
  Errors:   0
  Min:      639.37 ms
  Max:      672.09 ms
  Avg:      655.56 ms

======================================================================
```

## Статистика

Для каждого хоста программа выводит:

- **Host** - Адрес тестируемого сервера
- **Success** - Количество успешных запросов (статус 200-399)
- **Failed** - Количество запросов с ошибкой сервера (статус 400-599)
- **Errors** - Количество запросов с ошибкой соединения
- **Min** - Минимальное время ответа (в миллисекундах)
- **Max** - Максимальное время ответа (в миллисекундах)
- **Avg** - Среднее время ответа (в миллисекундах)

## Валидация входных данных

- **Hosts** - Проверка формата URL (должны начинаться с http:// или https://)
- **Count** - Проверка на положительное целое число
- **File** - Проверка существования файла и читаемости

## Обработка ошибок

Программа корректно обрабатывает следующие ошибки:

- Недоступность сервера (таймауты, ошибки соединения)
- Некорректный формат URL
- Некорректное значение count
- Отсутствующие или нечитаемые файлы
- Прерывание пользователем (Ctrl+C)

## Оптимизация производительности

Программа использует `ThreadPoolExecutor` для параллельного выполнения HTTP запросов, что значительно ускоряет процесс тестирования при большом количестве хостов и запросов.